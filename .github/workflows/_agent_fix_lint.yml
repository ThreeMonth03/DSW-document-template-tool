name: Fix PR 19 lint

on:
  push:
    branches:
      - codex/propose-fix-for-zip-bomb-vulnerability

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - name: Check out branch
        uses: actions/checkout@v7
        with:
          ref: codex/propose-fix-for-zip-bomb-vulnerability

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: make install-dev

      - name: Fix lint and remove helper
        run: |
          set -euo pipefail
          .venv/bin/python -m ruff check --fix --config config/ruff.toml \
            src/dsw_document_template_tool/tdk.py \
            tests/unit/test_tdk.py
          .venv/bin/python -m ruff format --config config/ruff.toml \
            src/dsw_document_template_tool/tdk.py \
            tests/unit/test_tdk.py
          rm -f .github/workflows/_agent_fix_lint.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "style: organize package-staging imports"
          git push origin HEAD:codex/propose-fix-for-zip-bomb-vulnerability
